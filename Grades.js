//const studentScores = [95, 78,85,60,45,92];
//const grades = [];

function assignGrade(studentScores){
    return studentScores.map((num)=> )
    if(studentScores[index] > 90)grades.push([A])})
    if(studentScores[index] > 80 && studentScores[index]< 89)grades.push(["B"])
    if(studentScores[index] > 70 && studentScores[index] < 79)grades.push(["C"])
    if(studentScores[index] > 60 && studentScores[index] < 69)grades.push(["D"])
     if(studentScores[index] < 60)grades.push(["F"])
    
    
}

    

console.log(grades)
    
//let answer = array.map((num)=> num % 2 == 1)


 //let answer = array.forEach((num)=>{arr.push (num* 2);})